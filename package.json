{
  "name": "script-uploader",
  "version": "1.0.0",
  "description": "A simple script uploader using Netlify Functions and GitHub Gists.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@octokit/core": "^4.1.0"
  }
}